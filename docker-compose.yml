version: '3.8'

# Mendefinisikan jaringan kustom agar kontainer bisa saling berkomunikasi
networks:
  msarch-net:
    driver: bridge

services:
  # Layanan untuk aplikasi Next.js Anda
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        # GANTI DENGAN PUID DAN PGID DARI PENGGUNA NAS ANDA
        # Jalankan 'id <username>' via SSH di NAS untuk mendapatkannya.
        - PUID=1026
        - PGID=100
    container_name: msarch-app-prod
    restart: unless-stopped
    env_file:
      - .env
    environment:
      # Mengarahkan path data internal kontainer ke /app/data
      # Ini memberitahu aplikasi untuk mencari database dan file upload di dalam folder ini.
      - DATABASE_PATH=/app/data
    volumes:
      # Map folder ./msarch-data di NAS Anda (host) ke /app/data di dalam kontainer.
      # Ini adalah bagian terpenting: semua data (database, file proyek, avatar) Anda
      # akan disimpan secara permanen di folder 'msarch-data' di NAS Anda.
      - ./msarch-data:/app/data
    networks:
      - msarch-net

  # Layanan untuk Cloudflare Tunnel
  tunnel:
    image: cloudflare/cloudflared:latest
    container_name: msarch-tunnel-connector
    restart: unless-stopped
    command: tunnel --no-autoupdate run
    environment:
      - TUNNEL_TOKEN=${CLOUDFLARED_TOKEN}
    # Menambahkan layanan 'tunnel' ke jaringan yang sama
    networks:
      - msarch-net
